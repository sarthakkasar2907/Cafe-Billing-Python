# Cafe-Billing-Python
It utilizes dictionaries to store menu prices, handles user input for orders (item and quantity), calculates the subtotal, applies an 8% sales tax, and generates a formatted final receipt."
